# Add your song here

Place your audio file in this folder and name it `our-song.mp3`.

The “play our song” button will play `/audio/our-song.mp3` and change to “pause the moment” while it is playing. If you prefer a different filename or audio format, update the `src` on the `<audio>` element in `app/page.tsx`.
